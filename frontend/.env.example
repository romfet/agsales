# Backend the dev server proxies to (default http://127.0.0.1:5050)
VITE_PROXY_TARGET=http://127.0.0.1:5050

# Optional: API base for the built SPA (leave empty to use same-origin /api)
VITE_API_BASE=

# Optional bearer token (placeholder auth, matches backend API_AUTH_TOKEN)
VITE_API_TOKEN=
