[pytest]
asyncio_mode = auto
markers =
    integration: requires a running PostgreSQL (set TEST_DATABASE_URL or DATABASE_URL)
testpaths = backend/tests
filterwarnings =
    ignore::pytest.PytestDeprecationWarning
